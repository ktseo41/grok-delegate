.DS_Store

# generated eval run artifacts (see evals/README.md — kept in the sibling *-workspace, not versioned)
opt-run/
loop.log
opt-report.html
*.local.log

# local checkout of xAI's open-source grok for source investigation (not versioned)
vendor-grok-build/
